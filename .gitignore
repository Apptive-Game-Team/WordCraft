bin/
obj/
*.user
.DS_Store
.worktrees/

# Vendored by Sim.csproj / Net.csproj on every build. The .meta files stay
# tracked so Unity keeps stable GUIDs; the assemblies themselves are output.
Client/Assets/Plugins/*.dll

# Contact sheets are regenerated by .art/make-sheets.sh
.art/sheets/
